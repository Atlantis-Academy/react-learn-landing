version: '3'

services:
  app:
    ports:
      - 3000:3000
      - 49153:49153
    build: ./
    command: yarn landing:dev
    restart: always
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
