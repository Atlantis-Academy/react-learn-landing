{
  "name": "react-learn-landing",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "start": "lerna run start",
    "lint": "mctl lint",
    "test": "mctl test",
    "commit": "mctl commit",
    "format": "mctl format",
    "typecheck": "mctl typecheck",
    "checkcommit": "mctl precommit",
    "landing:dev": "yarn workspace @landing/app start"
  },
  "husky": {
    "hooks": {
      "pre-commit": "mctl precommit",
      "commit-msg": "mctl commitmsg"
    }
  },
  "workspaces": [
    "landing/**/*"
  ]
}
